/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { RewarddisciplineService } from './rewarddiscipline.service';

describe('Service: Rewarddiscipline', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [RewarddisciplineService]
    });
  });

  it('should ...', inject([RewarddisciplineService], (service: RewarddisciplineService) => {
    expect(service).toBeTruthy();
  }));
});
